{% extends 'base.html.twig' %}

{% block title %}
    Edytuj galerie
{% endblock %}

{% block body %}
    <h1>Edytuj galerię</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input name="submit" type="submit" value="Zapisz" class="btn btn-primary"/>
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('gallery_index') }}" class="btn btn-link">
            Wróć do listy
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}